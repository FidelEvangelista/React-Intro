function TodoList(props) {
    return (
      <ul>
        {props.children}
      </ul>
    )
  }
  
  export {TodoList};
  
  // rfce = CREA COMPONENTE REAC 

  //Cerrar con ; todo export para no provocar errores